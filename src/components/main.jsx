import React from "react";

const main = () => {
  return (
    <div>
      <form>
        <label>Email</label>
        <input type="text"></input>

        <h1>Password</h1>
        <button>Submit</button>
      </form>
    </div>
  );
};

export default main;
